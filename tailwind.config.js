/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/main/resources/templates/**/*.html" // Scan all HTML files in templates
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
